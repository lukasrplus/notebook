<h1>Hi Lukas!</h1>

<ul>
  <% @notes.each do |n| %>
    <%= render 'note', :note => n %>
  <% end %>
</ul>

<%= form_for Note.new, remote: true do |f| %>
      <%= f.text_field :content, placeholder: 'I am GREEEEN. Add a new aha! here', size: 50 %>
    <% end %>
